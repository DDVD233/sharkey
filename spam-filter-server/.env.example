# Copy to .env and fill in.

# Shared secret between Sharkey and the classifier. Set the SAME value in
# Sharkey admin settings (spamFilterApiKey). Generate e.g. `openssl rand -hex 32`.
CLASSIFIER_API_KEY=

# Optional: a Hugging Face token if the model requires accepting a license.
HUGGING_FACE_HUB_TOKEN=
